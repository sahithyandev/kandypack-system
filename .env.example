# JWT Configuration
JWT_SECRET=your_jwt_secret_minimum_32_characters_long_change_this

# Database Configuration (for production)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_secure_database_password_here
POSTGRES_DB=kandypack
DATABASE_URL=postgresql://postgres:your_secure_database_password_here@db:5432/kandypack

# Backend Configuration
PORT=2000
NODE_ENV=development

# Frontend Configuration
NEXT_PUBLIC_API_URL=http://localhost:2000

# Production Notes:
# - Generate secure JWT_SECRET: openssl rand -base64 64
# - Generate secure POSTGRES_PASSWORD: openssl rand -base64 32
# - Update DATABASE_URL with the same password as POSTGRES_PASSWORD
# - For production deployment, these values should be set in GitHub Secrets
